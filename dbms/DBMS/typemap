TYPEMAP

DBT			T_DATUM
DBMS			T_PTROBJ

INPUT
T_DATUM
	$var.size = (int)PL_na;
        $var.data = SvPV($arg, PL_na);

OUTPUT
T_DATUM
	sv_setpvn($arg, $var.data, $var.size);
	if ($var.data && $var.size) safefree($var.data);
